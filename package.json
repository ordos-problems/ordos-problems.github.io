{
  "scripts": {
    "compile": "python3 build/compile.py",
    "render": "node build/render_math.js",
    "build": "python3 build/compile.py && node build/render_math.js && python3 build/build_site.py"
  },
  "dependencies": {
    "katex": "^0.17.0"
  }
}
